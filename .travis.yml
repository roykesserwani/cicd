sudo: required


services:
  - docker

jobs:
  language: bash
  include:
    - stage: "Docker 1"
    - name:  "Docker compose up"
    - script: docker-compose up



    - stage: "Docker 2"
    - name:  "Docker list processes"
    - script: docker ps -a
